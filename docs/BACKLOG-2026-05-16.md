# Weekly Backlog — 2026-05-16

Automated code review of branch `tier3` (last commit 2026-05-03, `6d54e47`).
Scope: uncommitted working tree + build/test/lint health.

## TL;DR

The `internal/...` codebase is healthy — **all unit/integration tests pass** (incl. raft, ~126s).
But the working tree on `tier3` is currently **un-mergeable**: the CLI does not compile
and CI lint cannot run. Two blockers must be fixed before anything else this week.

---

## P0 — Blockers (do first, in order)

### 1. Fix corrupted `cmd/konsulctl/client.go` (build broken)

A botched regex/sed find-replace left the literal backreference `\1` in 13 places:

```go
req, err := http.NewRequest(\1, http.NoBody)   // invalid Go — does not compile
```

Affected lines: 516, 546, 795, 821, 847, 1026, 1118, 1262, 1389, 1416, 1443, 1503, 1530.

Impact:
- `go build ./...`, `make build-cli` fail.
- `go test ./...` fails to build `cmd/konsulctl` → **CI test job is red**.
- Only `cmd/konsulctl` is affected; `internal/...` and `cmd/konsul` build clean.

Fix: the intent was `http.NewRequest("GET"/"POST"/"PUT"/"DELETE", url, nil)` →
`http.NewRequest(http.MethodGet/Post/Put/Delete, url, http.NoBody)`. Restore the
correct HTTP method constant per call site (recover the original method from
`git show HEAD:cmd/konsulctl/client.go` for each line) and re-run the sed with the
capture group actually substituted, or fix the 13 sites by hand. Verify with
`go build ./... && go test ./cmd/...`.

Owner: _unassigned_ · Est: 1–2h · Due: 2026-05-17

### 2. Resolve `.golangci.yml` schema vs. CI version mismatch (CI lint broken)

Recent commits `d8bb7e8`/`6d54e47` reverted `.golangci.yml` to **v1 format** (targeting
golangci-lint v1.64.x). But:
- CI `.github/workflows/go.yaml:44` uses `golangci/golangci-lint-action@v6` with
  `version: latest` → installs golangci-lint **v2.x**.
- Locally installed: v2.10.1.
- Result: `can't unmarshal config ... 'output.formats' expected a map, got 'slice'`.
  Lint fails both locally and in CI.

Pick one:
- **(a) Recommended:** keep modern tooling — run `golangci-lint migrate` to convert
  `.golangci.yml` to v2 schema; keep CI on `version: latest`.
- **(b)** pin CI to v1: `version: v1.64.8` in `go.yaml`, and document/standardize the
  local install version in the README/Makefile.

Then run `golangci-lint run ./...` clean (after P0-1 so `cmd/konsulctl` parses).

Owner: _unassigned_ · Est: 1–2h · Due: 2026-05-17

---

## P1 — This week

### 3. Commit the (otherwise good) lint-hardening pass

Once P0-1 and P0-2 are green, the remaining working-tree diff is a legitimate, mostly
mechanical cleanup and should be committed:
- `http.NoBody` instead of `nil` request bodies.
- `//nolint:gosec` annotations with justifications (G115 int conversions, G402
  `InsecureSkipVerify` dev/test option, gRPC TLS MinVersion-set-by-caller).
- **Security improvement** in `internal/handlers/acl.go`: policy files now written
  `0o600` instead of `0644` (G306) — keep this.
- Style: `if/else` → `switch` (`internal/dns/server.go`, `internal/audit/helpers.go`),
  `x = x + y` → `x += y`, `fmt.Fprintf` over `io.WriteString(fmt.Sprintf(...))`,
  named returns, dead branch removal in `internal/handlers/agent_handlers.go`
  (the full/delta sync branches were identical — behavior preserved).

Action: split into a focused commit, e.g. `chore(lint): http.NoBody, gosec
annotations, 0o600 policy perms, gofmt/staticcheck cleanup`. Run `make test` first.

Owner: _unassigned_ · Est: 30m · Due: 2026-05-19

### 4. Verify stray git stash is not lost work

`git stash list` shows `stash@{0}: On 1st: claude`. Inspect (`git stash show -p
stash@{0}`); either apply, or drop if obsolete. Don't leave WIP orphaned on a
feature branch.

Owner: _unassigned_ · Est: 15m · Due: 2026-05-19

### 5. Add a CI guard so a non-compiling commit can't go green again

P0-1 reached the working tree because nothing caught it. Confirm `.github/workflows/
go.yaml` runs `go build ./...` (or `go vet ./...`) as an explicit step that fails the
job, independent of the test step, so a parse error in any package is caught fast.

Owner: _unassigned_ · Est: 30m · Due: 2026-05-21

---

## P2 — Backlog / opportunistic

- **18 TODO/FIXME/XXX** in non-test code under `internal/` and `cmd/`. Triage into
  the main `docs/BACKLOG.md`; convert anything load-bearing into tracked items.
- `internal/handlers/agent_handlers.go`: delta sync still returns the **full** data
  set (the now-removed branch made this explicit). Real incremental sync since
  `LastSyncIndex` remains unimplemented — flag against the agent roadmap.
- Several GraphQL subpackages have **no test files** (`graphql`, `dataloader`,
  `model`, `scalar`, `middleware`). Consider minimal coverage for the scalar
  marshalers touched this pass.

---

## Health snapshot (2026-05-16)

| Check | Result |
|---|---|
| `go build ./internal/... ./cmd/konsul/...` | ✅ pass |
| `go build ./cmd/konsulctl` | ❌ fail (P0-1) |
| `go vet ./internal/... ./cmd/konsul/...` | ✅ pass |
| `go test ./internal/...` | ✅ all pass (raft ~126s) |
| `golangci-lint run` | ❌ config schema mismatch (P0-2) |

Definition of done for the week: branch `tier3` builds fully, `make test` green,
`golangci-lint run` green, lint-cleanup committed, stash resolved.
