apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konsul.fullname" . }}-test-connection"
  labels:
    {{- include "konsul.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konsul.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
